markdown: redcarpet
highlighter: null

url: "http://ameyer.me"
baseurl:

redcarpet:
  extensions: [strikethrough, tables, footnotes, smart]

sass:
  sass_dir: public/css
  style: :compressed

gems:
  - jekyll-archives
  - jekyll-sitemap

jekyll-archives:
  enabled: all
  layouts:
    year: year-archives
    tag: tag-archives
    category: category-archives
    month: month-archives
    day: day-archives
  permalinks:
    year: '/:year/'
    month: '/:year/:month/'
    day: '/:year/:month/:day/'
    tag: '/tag/:name/'
    category: '/:name/'

exclude:
  - Gemfile
  - Gemfile.lock
